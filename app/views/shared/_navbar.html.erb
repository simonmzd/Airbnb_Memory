<div class="navbar navbar-expand-sm navbar-light border-bottom" style="background-color: #26A69A;">
  <div class="container-fluid px-3">
    <div class="d-flex w-100 justify-content-between align-items-center">

      <div class="me-auto">
        <%= link_to root_path, class: "navbar-brand mb-0" do %>
          <%= image_tag "Logo.png", class: "img-fluid", style: "max-width: 50px;" %>
        <% end %>
      </div>

      <% if current_page?(root_path) || params[:query].present? %>
        <div class="flex-grow-1 d-flex justify-content-center">
          <%= form_with url: memories_path, method: :get, class: "w-50 position-relative" do %>
            <%= text_field_tag :query,
                params[:query],
                class: "form-control form-control-sm text-start ps-5 rounded-3",
                placeholder: "Rechercher", aria: { label: "Search" }
            %>
            <i class="fa fa-search position-absolute top-50 start-0 translate-middle-y ms-2" style="color: #26A69A;"></i>
          <% end %>
        </div>
      <% end %>



      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>


      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto">
          <% if user_signed_in? %>
            <li><%= link_to "Tableau de bord", dashboard_path, class: "nav-link fw-bold" %></li>
            <li class="nav-item">
              <%= link_to "Déconnexion", destroy_user_session_path, data: { turbo_method: :delete }, class: "nav-link fw-bold" %>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to "Connexion", new_user_session_path, class: "nav-link" %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>
