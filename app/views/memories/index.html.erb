<% @memories.each do |memorie| %>
  <%= link_to memorie.title, memory_path(memorie) %>
  <% if memorie.image.attached? %>
    <%= image_tag url_for(memorie.image) %>
  <% else %>
    <%= cl_image_tag(memorie.images.first.key,
        width: 400, height: 300, crop: :fill) %>
  <% end %>
<% end %>
